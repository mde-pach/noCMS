{
  "name": "@nocms/controls",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Editor control derivation: introspect a valibot props/field schema into a serializable ControlDescriptor tree, and walk that tree to enumerate content-text and array-item paths. The single schema→control mapper shared by component props and collection fields.",
  "sideEffects": false,
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "valibot": "^1.4.1"
  },
  "devDependencies": {
    "@nocms/core": "workspace:*"
  }
}
