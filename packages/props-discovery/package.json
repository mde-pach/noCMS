{
  "name": "@nocms/props-discovery",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Parses a component's TypeScript types to auto-derive editor controls — no annotation DSL. The type determines the control (string→text, literal-union→select, ReactNode→slot, handler→action-binding). Bridges parsed types with an optional thin field-config.",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nocms/core": "workspace:*",
    "typescript": "^5.7.0"
  }
}
