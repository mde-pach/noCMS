{
  "name": "@nocms/router",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "The ① runtime routing model: a pure route table + matcher built from content entries (using core's canonical path↔route mapping), plus an optional lightweight History-API navigation surface for soft navigation. Pure route logic is DOM-free and unit-testable; side effects (History, link clicks) are isolated at the edges.",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nocms/core": "workspace:*"
  }
}
