{
  "name": "@nocms/renderer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "THE single rendering engine. Renders the MDX→Preact component tree. Universal: the exact same tree runs live in the browser (editor preview) and is prerendered to static HTML in Node at publish (preact-render-to-string). One component model, one renderer — what you preview is what you publish.",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nocms/core": "workspace:*",
    "preact": "^10.27.0",
    "preact-render-to-string": "^6.6.0",
    "@mdx-js/mdx": "^3.1.0",
    "@mdx-js/preact": "^3.1.0"
  }
}
