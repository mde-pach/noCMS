{
  "name": "@nocms/tokens",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "The flat, one-token-per-line file is the source of truth. Parses it, compiles tokens to CSS custom properties (runtime theming, no rebuild), and generates nested W3C DTCG for interop (Style Dictionary, Tokens Studio, Penpot). Flat→DTCG, never the reverse.",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@nocms/core": "workspace:*"
  }
}
